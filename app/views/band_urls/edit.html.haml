.row
  .small-24.columns.page_top
    = t('generic.editing', :name => @band_url.name)

.row
  .small-24.columns
    = render 'form'

.row
  .small-24.columns
    = link_to t('cancel'), @band_url, :class => 'button'
    = link_to t('delete'), @band_url, :method => :delete, :data => {:confirm => t('are_you_sure')}, :class => 'button'

