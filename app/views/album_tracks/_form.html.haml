= simple_form_for(@album_track) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :album
    = f.input :tracks, :as => :text, :input_html => {:rows => 10}

  .form-actions.float-right
    = f.button :submit, :class => 'button'

