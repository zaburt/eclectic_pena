= simple_form_for(@album_cover) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :album

  .form-actions.float-right
    = f.button :submit, :class => 'button'

