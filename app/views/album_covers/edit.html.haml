.row
  .small-24.columns.page_top
    = t('generic.editing', :name => @album_cover.name)

.row
  .small-24.columns
    = render 'form'

.row
  .small-24.columns
    = link_to t('cancel'), @album_cover, :class => 'button'
    = link_to t('delete'), @album_cover, :method => :delete, :data => {:confirm => t('are_you_sure')}, :class => 'button'

