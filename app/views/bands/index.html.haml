.row
  .small-24.columns.page_top
    = t('bands')

.row
  .small-24.columns
    %table#bands_table.serverside_datatables{'data-source' => bands_path}
      %thead
        %tr
          %th
            = t('activerecord.attributes.band.name')
          %th
            = t('activerecord.attributes.band.country')
          %th
            = t('activerecord.attributes.band.website')
          /%th.datatable_no_search.datatable_no_order
          /%th.datatable_no_search.datatable_no_order
          /%th.datatable_no_search.datatable_no_order

      %tbody
      /  - @bands.each do |band|
      /    %tr
      /      %td= band.name
      /      %td= band.country
      /      %td= band.website
      /      %td= link_to 'Show', band
      /      %td= link_to 'Edit', edit_band_path(band)
      /      %td= link_to 'Destroy', band, :method => :delete, :data => { :confirm => 'Are you sure?' }

    %br

    = link_to t('generic.new_model', :name => Band.model_name.human), new_band_path, :class => 'button'

