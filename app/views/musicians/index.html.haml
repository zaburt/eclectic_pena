.row
  .small-24.columns.page_top
    = t('musicians')

.row
  .small-24.columns
    %table#musicians_table.serverside_datatables{'data-source' => musicians_path}
      %thead
        %tr
          %th
            = t('activerecord.attributes.musician.name')
          %th.datatable_no_search
            = t('activerecord.attributes.musician.birthdate')
          %th
            = t('activerecord.attributes.musician.country')
          %th
            = t('activerecord.attributes.musician.website')
          /%th.datatable_no_search.datatable_no_order
          /%th.datatable_no_search.datatable_no_order
          /%th.datatable_no_search.datatable_no_order

      %tbody
      /  - @musicians.each do |musician|
      /    %tr
      /      %td= musician.name
      /      %td= musician.birthdate
      /      %td= musician.country
      /      %td= link_to 'Show', musician
      /      %td= link_to 'Edit', edit_musician_path(musician)
      /      %td= link_to 'Destroy', musician, :method => :delete, :data => { :confirm => 'Are you sure?' }

    %br

    = link_to t('generic.new_model', :name => Musician.model_name.human), new_musician_path, :class => 'button'

