.row
  .small-24.columns.page_top
    = t('generic.editing', :name => @musician.name)

.row
  .small-24.columns
    = render 'form'

.row
  .small-24.columns
    = link_to t('cancel'), @musician, :class => 'button'
    = link_to t('images'), musician_photos_path(:musician_id => @musician.id), :class => 'button'
    = link_to t('labels'), edit_musician_path(@musician, :edit_labels => true), :class => 'button'
    = link_to t('delete'), @musician, :method => :delete, :data => {:confirm => t('are_you_sure')}, :class => 'button'

