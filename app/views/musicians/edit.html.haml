.row
  .small-24.columns.page_top
    = t('generic.editing', :name => @musician.name)

.row
  .small-24.columns
    = render 'form'

.row
  .small-24.columns
    = link_to t('show'), @musician, :class => 'button'
    = link_to t('back'), musicians_path, :class => 'button'

