.row
  .small-24.columns.page_top
    = t('generic.editing', :name => @album_type.name)

.row
  .small-24.columns
    = render 'form'

.row
  .small-24.columns
    = link_to t('cancel'), @album_type, :class => 'button'
    = link_to t('delete'), @album_type, :method => :delete, :data => {:confirm => t('are_you_sure')}, :class => 'button'

