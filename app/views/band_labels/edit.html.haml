.row
  .small-24.columns.page_top
    = t('generic.editing', :name => @band_label.name)

.row
  .small-24.columns
    = render 'form'

.row
  .small-24.columns
    = link_to t('cancel'), @band_label, :class => 'button'
    = link_to t('delete'), @band_label, :method => :delete, :data => {:confirm => t('are_you_sure')}, :class => 'button'

