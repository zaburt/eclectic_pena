.row
  .small-24.columns.page_top
    = t('users')

.row
  .small-24.columns
    %table
    %table#users_table.serverside_datatables{'data-source' => users_path}
      %thead
        %tr
          %th
            = t('activerecord.attributes.user.name')
          %th
            = t('activerecord.attributes.user.email')
          %th
            = t('activerecord.attributes.user.sign_in_count')
          %th
            = t('activerecord.attributes.user.current_sign_in_ip')
          %th
            = t('activerecord.attributes.user.current_sign_in_at')

      %tbody
      /  - @users.each do |user|
      /    %tr
      /      %td= link_to 'Show', user
      /      %td= link_to 'Edit', edit_user_path(user)
      /      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

    %br

    = link_to t('generic.new_model', :name => User.model_name.human), new_user_path, :class => 'button'

